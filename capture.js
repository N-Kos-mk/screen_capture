fetch('https://cdnjs.cloudflare.com/ajax/libs/html2canvas/0.4.1/html2canvas.js').then(response=>response.text()).then(data=>{eval(data);const nextCode=` html2canvas(document.body,{ onrendered: function(canvas){ var imgData = canvas.toDataURL(); var blob = dataURItoBlob(imgData); var url = URL.createObjectURL(blob); var a = document.createElement('a'); a.href = url; a.download = 'screenshot.png'; a.click(); URL.revokeObjectURL(url); } }); function dataURItoBlob(dataURI) { var byteString = atob(dataURI.split(',')[1]); var mimeString = dataURI.split(',')[0].split(':')[1].split(';')[0]; var ab = new ArrayBuffer(byteString.length); var ia = new Uint8Array(ab); for (var i = 0; i < byteString.length; i++) { ia[i] = byteString.charCodeAt(i); } var blob = new Blob([ab], { type: mimeString }); return blob; } `;eval(nextCode)}).catch(error=>{console.error('There was a problem loading the JavaScript files:',error)})
